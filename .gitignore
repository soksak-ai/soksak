.task
bin
frontend/dist
frontend/node_modules
build/linux/appimage/build
build/windows/nsis/MicrosoftEdgeWebview2Setup.exe
.DS_Store
# Mobile generated (per-machine; overlays hold absolute paths)
build/ios/xcode/overlay.json
build/ios/xcode/gen/
build/android/overlay.json
build/android/gen/

# Rooted: this is the one directory at the top of the repository, not any
# directory that happens to be called secret. Without the slash it also swallowed
# core/secret — the package that holds the vault, which is code and belongs here.
/secret/
bin/
