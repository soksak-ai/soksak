.task
bin
frontend/dist
frontend/node_modules
# vite writes a cache here when it runs from the module root rather than frontend/.
node_modules
.pnpm-store
build/linux/appimage/build
build/windows/nsis/MicrosoftEdgeWebview2Setup.exe
.DS_Store
# Mobile generated (per-machine; overlays hold absolute paths)
build/ios/xcode/overlay.json
build/ios/xcode/gen/
build/android/overlay.json
build/android/gen/

/local/
bin/

# Frames a gate records on every run — 23MB per run, regenerated, and never the pass mark.
# What passes or fails is the numbers the gate prints beside them.
evidence/scenarios/
/evidence/

# Local iteration against an uncommitted spec module. Never committed.
go.work
go.work.sum
