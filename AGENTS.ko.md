---
kind: translation
status: active
canonical: ./AGENTS.md
---

# 이 저장소에서 일하는 방식

이 프로젝트의 개발 규율입니다. 코드를 읽거나 쓰거나 고치기 전에, 그리고 모든 commit·merge·릴리즈
전에 읽습니다.


## 1. 프로젝트 변수

물어보지 않고 저장소에서 발견합니다.

| 변수 | 값 |
|---|---|
| 저장소 root | 이 `AGENTS.md` 와 `go.mod` 를 담은 체크아웃 |
| 관측 인터페이스 | `bin/sok`(control plane 클라이언트), `bin/soksak`(애플리케이션) |
| 테스트 실행기 | `go test`(Go), `vitest run`(TypeScript) |
| 빌드 toolchain 계약 | [`docs/tech/BUILD-TOOLCHAIN.md`](docs/tech/BUILD-TOOLCHAIN.md) |
| 테스트 위치 | 코어 테스트는 코어에, 플러그인 테스트는 각 플러그인에 |
| commit 언어 | 영어 |
| i18n 프레임워크 | 자체 키 표: `frontend/src/i18n.ts` 와 `i18n.ko.ts` / `i18n.en.ts`. Go 에는 아직 없음 |
| 기본·지원 로케일 | `ko` / `ko`, `en` |

경로는 선언과 발견으로 해석합니다. 심볼릭 링크는 쓰지 않습니다. 모듈을 추측한 상대 경로로 잇지
않습니다.

저장소와 branch 변경은 [`docs/tech/REPOSITORY-GOVERNANCE.md`](docs/tech/REPOSITORY-GOVERNANCE.md)
를 따릅니다.

## 2. 누적 검사

G0 부터 G5 는 구현 단계가 아니라 완료 기준의 그래프입니다. 그 사이에 수십 개의 가설이 생깁니다.

**누적하는 것**: 확정된 제품 규칙 · 결함을 재현하는 수치 RED 테스트 · 검증된 관측 인터페이스 ·
누적된 모든 검사를 통과하는 구현 · 현재 정본 문서.

**누적하지 않는 것**: 자기 좁은 테스트만 통과하는 구현 · 실제 화면에서 실패하는 구현 · 이후 어느
단계도 지지하지 않는 추상화 · 임시 대체 경로·호환 경로·우회 · 실패한 구현을 떠받치는 추가 편법.

다음 변경을 실패한 가설 위에 쌓지 않습니다. 실패한 구현을 제거하고, 마지막으로 검증된 지점으로 돌아간
뒤, 새 가설을 시작합니다. 이미 commit 했다면 제거 commit 을 그 위에 쌓습니다.

commit 하나는 검증된 수직 계약이지 개발 단계가 아닙니다.

### 되돌리기

1. 해로운가? 즉시 되돌립니다.
2. 해롭지 않다면, 그 변경 자체가 옳은가?
   - 틀렸거나 불필요하면 제거합니다.
   - 옳으면 유지합니다. 원래 작업과 무관하다면 그 작업의 이유로 commit 하지 않습니다. 진짜 이유를
     적어 따로 commit 합니다.

판정 기준은 해로운가·옳은가이지 의도했는가가 아닙니다. 코드는 옳게 들어가고, 기록은 사실을
진술합니다.

## 3. 검증과 관측

### 3-1. RED 에서 GREEN 으로

1. 문제를 재현하는 RED 테스트를 씁니다. 재현할 수 없으면 그것을 가정하는 RED 테스트를 씁니다.
2. RED 가 실제로 실패하는지 확인합니다. 통과하는 RED 는 RED 가 아닙니다.
3. 고칩니다.
4. 같은 테스트가 GREEN 이 되는지 확인합니다.

RED 없는 수정에는 증거가 없습니다. 증거 없이 완료를 보고하지 않습니다.

### 3-2. 캡처와 숫자

- 개발 중에 화면 캡처와 녹화를 씁니다. 보지 않고 개발하는 것은 없습니다.
- 캡처는 창의 포커스를 가져가지 않습니다. 포커스를 빼앗는 것은 사용자의 머신을 빼앗는 것입니다.
- **캡처는 통과 판정이 아닙니다.** 사람이 읽어야 합니다.
- 관측한 모든 현상은 기계적인 통과·실패 확인으로 바꿉니다.

| 관측한 것 | 수치 근거 |
|---|---|
| 애니메이션이 튄다 | 프레임별 좌표와 시각. 전후 비교 |
| DOM 과 webview 합성이 어긋난다 | 두 위치를 측정. 같은가 아닌가 |
| 느리다 | 단계별 시간. 임계값 대비 판정 |
| 가끔 실패한다 | 실패 조건을 기록. 반복 성공률로 판정 |

근거를 만들 수 없으면 그 사실과 이유를 보고합니다.

### 3-3. 명령 노출

"검증할 명령이 없었다" 는 할 말이 아닙니다. 명령을 노출하는 것이 작업의 일부입니다. 상태를 읽는
status, 동작을 일으키는 명령, 측정을 기록하는 로그 지점입니다. 그런 인터페이스는 누적하며, 만들었다
지우지 않습니다.

### 3-4. 멱등

- 동작하는 것처럼 보이게 덧대지 않습니다.
- 이벤트는 관측 가능하고 기계적이며, 발생을 찾아다니지 않고 알 수 있습니다. 찾아야만 하는 구조는
  실패입니다.
- 다음 실행도 같게 동작합니다.

### 3-5. 스크립트와 테스트 배치

- 일회용 스크립트를 만들지 않습니다. 스크립트가 할 일은 기능으로 넣습니다. 멱등하게, 스크립트를 쓰는
  것과 같은 시간에.
- 단위 테스트와 종단 간 테스트는 버전 관리합니다.
- 코드와 테스트는 별도 파일입니다. `x.go`/`x_test.go`, `x.ts`/`x.test.ts`. 커진 파일은 크기가 아니라
  담고 있는 규칙으로 나눕니다.
- 코어 테스트는 코어 폴더에, 플러그인 테스트는 각 플러그인 폴더에.

### 3-6. 파괴적 명령의 안전한 순서

찾기와 바꾸기를 처음부터 합치지 않습니다. 잘못 걸린 하나가 여러 파일을 망칩니다.

1. 검색만 먼저 실행합니다(`grep`, `git grep`, `rg --files-with-matches`).
2. 결과 전체를 읽습니다. 개수와 대상을 예상과 대조합니다.
3. 그다음에만 변경과 합칩니다.
4. 대상이 많으면 하나에 적용하고 확인한 뒤 넓힙니다.

`xargs sed -i`, `find -exec rm`, 일괄 치환은 3단계 이후입니다. 2026-08-15 실측: 확인하지 않은
`sed -i` 와 `perl -0pi` 가 한 세션에서 파일 넷을 조용히 망가뜨렸습니다.

### 3-7. 표준

표준에 이르지 못하자 표준을 낮추는 것은 배신입니다. 하지 않습니다.

표준 자체가 틀렸다면 제기하고 고칩니다. 표준을 낮추는 것과 틀린 표준을 고치는 것은 다른 행위이며, 둘을
구분하지 못한다면 그것은 전자입니다.

## 4. 구조

### 4-1. 코어와 플러그인

코어는 어떤 것에도 강하게 결합돼 있지 않습니다. 모든 것이 열려 있고 규칙이 있으며, 플러그인은 선언된
인터페이스로 서로 만납니다.

**코어가 노출하는 것**: 모든 DOM 노드 — 노출된 노드는 밖에서 클릭·드래그·이벤트 발행이 됩니다.
모든 명령 — 연결은 명령을 지납니다. 모든 status.

**플러그인**: 플러그인 사이에 강한 결합이 없습니다. 관계는 선언된 의존이고, 데이터는 선언된
인터페이스로만 넘어갑니다.

**어디에 속하는가**: 공통인 것은 코어로 가서 플러그인이 다시 만들지 않게 합니다. 플러그인 자신의
기능은 코어에 넘기지 않습니다. 불분명하면 판정은 질문 하나입니다 — 여러 플러그인이 같은 것을
필요로 하는가? 예: 코어. 아니오: 플러그인.

**금지된 결합**: 추측한 상대 경로 금지, 심볼릭 링크 금지. 어떤 플러그인도 다른 플러그인의 내부를 읽지
않습니다. 코어는 특정 플러그인이 존재한다고 가정하지 않습니다. 강제:
[`docs/tech/ARCHITECTURE.md`](docs/tech/ARCHITECTURE.md) 의 C1–C3.

### 4-2. 단순함

지금 요구를 완전히 만족하는 가장 단순한 구현.

만들지 않는 것: 불확실한 미래를 위한 추상화, 분기가 하나뿐인 설정 스위치, 호출자가 하나뿐인 간접 계층,
나중을 위한 확장 지점.

**수직으로 키웁니다**: 종단 간으로 동작하는 가장 작은 버전에서 시작합니다. 동작하는 제품 위에 기능을
하나씩 더합니다. 동작하는 제품을 미완성 복잡함과 바꾸지 않습니다.

**파일 나누기**: 판정 기준은 책임이지 줄 수가 아닙니다. 한 파일에 관심사가 둘이면 파일도 둘입니다.

### 4-3. 하위 호환

호환 계층, 대체 경로, 마이그레이션을 만들지 않습니다. 옛 경로는 삭제합니다.

이것은 내부 코드에 대한 규칙입니다. 발행된 API 의 호환성 파괴는 숨기지 않습니다. 정직하게 semver
MAJOR 로 표시하고 진행합니다(§7-5).

**규칙이 다스리는 것은 코드 경로이며, 저장된 데이터는 코드 경로가 아닙니다.** 옛 경로를 삭제한다는
것은 이 빌드가 더 이상 쓰지 않는 모양을 읽는 일을 멈춘다는 뜻입니다. 디스크의 기록을 그 작성자를
대신해 다시 만든다는 뜻이 아니며, 옛 기록에 없는 것을 지어내는 대체 경로를 허락하지도 않습니다. 이
빌드가 읽을 수 없는 기록은 이름과 함께 거부하고 그대로 두며, 그 기록만 잃습니다 — RESTORE R1.

규칙을 어기는 방법은 둘이고, 그중 하나만 어기는 것처럼 보입니다.

- `snap.id ?? mint()` 는 마이그레이션입니다. 신중함처럼 읽히지만 두 번째 코드 경로이고, 옛 기록
  하나하나가 그것을 살려 둡니다. 아무것도 그것을 지우지 않으며, 그것이 감추는 결함은 이름이 바뀐
  id 입니다.
- 기록을 거부하는 것은 마이그레이션이 아닙니다. 리더 하나, 모양 하나, 그리고 읽을 수 없는 것은 추측
  대신 보고합니다.

2026-08-16 실측: id 가 생기기 전에 쓰인 스냅샷을 위해 split 노드 대체 경로를 추가했습니다. 먼저 보이게
만들어 부팅 사실로 보고했는데, 보이는 것도 여전히 두 번째 경로였습니다. 제거했고, 이제 모양 검사가 그
기록을 거부합니다.

### 4-4. 의존

순서대로:

1. 이미 있는 의존으로 되는가? 문서와 타입 정의를 읽습니다. 안 된다고 단정하지 않습니다.
2. 복잡함을 줄이거나 신뢰성을 높이는 검증된 라이브러리가 있는가? 그것을 택합니다.
3. 분명한 이유가 있는가? 없으면 흔한 기능을 다시 구현하지 않습니다.

도구 버전과 빌드 진입점은 `docs/tech/BUILD-TOOLCHAIN.md` 를 따릅니다. 테스트와 스크립트는 소유
manifest 를 읽습니다. 현재 버전을 복사하거나 ambient 대체 바이너리를 고르지 않습니다.

### 4-5. 폴링

폴링은 직접적인 방법이 하나도 없을 때의 대안입니다. 기본 수단이 아닙니다.

순서: 이벤트와 구독 → 콜백과 훅 → 명시적 명령 호출 → 폴링.

폴링을 쓸 때는 주석 한 문장으로 왜 쓰는지와 직접 경로가 왜 불가능한지를 적습니다.

### 4-6. 임시방편 금지

구조는 장기를 보고 정합니다. 지금은 동작하지만 나중에 교체해야 하는 임시방편은 받지 않습니다.

옳은 방법이 불가능해 우회를 쓸 때는 정확히 적습니다. 옳은 방법이 무엇인지, 지금 왜 불가능한지, 어떤
우회를 택했는지, 돌아가려면 무엇이 바뀌어야 하는지입니다. 적지 않은 우회는 허용하지 않습니다.

## 5. i18n

i18n 은 나중에 넣을 수 없습니다. 문제는 하드코딩된 문자열 하나가 아니라, 이어 붙여 만든 문장 구조가
번역 불가능해진다는 점입니다.

### 5-1. 문자열

사용자에게 보이는 문자열은 코드에 쓰지 않습니다. 키로 참조하고 리소스 번들에서 해석합니다. 문장을
코드에서 조립하지 않습니다. 어순이 언어마다 다릅니다. placeholder 를 씁니다.

```
BAD:  t("delete") + " " + count + " " + t("items")
GOOD: t("file.delete_confirm", { count })
      ko: "파일 {count}개를 삭제합니다"
      en: "Delete {count} files"
```

키는 값이 아니라 의미를 따라 이름 지은 영어 계층입니다.

```
BAD:  t("삭제하시겠습니까")   // 원문을 키로 쓰면 문구가 바뀔 때 전부 깨진다
BAD:  t("msg_042")            // 의미를 담지 않는다
GOOD: t("file.delete_confirm")
```

같은 원문이라도 문맥이 다르면 다른 키입니다. 영어 "Open" 은 한 곳에서는 동사이고 다른 곳에서는
형용사입니다.

### 5-2. 복수

`if (n === 1)` 을 쓰지 않습니다. 복수 규칙은 언어마다 다릅니다. 한국어는 하나, 영어는 둘, 러시아어는
셋, 아랍어는 여섯입니다. 프레임워크의 복수 기능(ICU MessageFormat 또는 동등물)을 씁니다.

```
BAD:  count === 1 ? t("one_item") : t("many_items")
GOOD: t("item.count", { count })   // {count, plural, one {...} other {...}}
```

### 5-3. 로케일 의존 데이터

손으로 형식을 만들지 않습니다. 플랫폼의 로케일 API 를 씁니다.

| 대상 | 금지 | 사용 |
|---|---|---|
| 날짜와 시각 | `` `${y}-${m}-${d}` `` | `Intl.DateTimeFormat` 또는 동등물 |
| 숫자 | 손으로 만든 천 단위 구분 | `Intl.NumberFormat` |
| 통화 | 하드코딩한 기호 | `Intl.NumberFormat` currency style |
| 정렬 | 코드 포인트 비교 | `Intl.Collator` |
| 상대 시간 | "3일 전" 을 조립 | `Intl.RelativeTimeFormat` |

시각은 UTC 로 저장하고 표시할 때 변환합니다. 지역 시각은 저장하지 않습니다. 통화 값은 통화 코드와 함께
저장하며, 숫자만 저장하지 않습니다.

### 5-4. 레이아웃

**텍스트는 늘어납니다.** 번역문은 원문보다 깁니다(영어에서 독일어로 30% 이상). 텍스트를 고정 폭
컨테이너에 넣지 않습니다.

**RTL.** 물리 속성이 아니라 논리 속성을 씁니다.

```
BAD:  margin-left, padding-right, text-align: left, left: 0
GOOD: margin-inline-start, padding-inline-end, text-align: start, inset-inline-start: 0
```

방향이 의미를 담은 아이콘(뒤로, 진행)은 RTL 에서 좌우를 뒤집습니다. 텍스트를 이미지에 굽지 않습니다.

### 5-5. 입력과 저장

- 어디서나 UTF-8.
- 길이 검사는 바이트 수와 문자 수를 구분합니다. 사용자에게 보이는 한계는 grapheme 을 셉니다.
- 이름, 주소, 전화번호, 우편번호에 특정 국가의 형식을 강요하지 않습니다. 이름을 성과 이름 둘로 나누는
  것, 우편번호 필수, 주 필수는 모두 지역 가정입니다.
- 대소문자 변환은 로케일을 명시합니다. 터키어 `i` 는 그것 없이는 깨집니다.

### 5-6. 로케일 선택

- 번역되지 않은 키는 기본 로케일로 대체합니다. 키 문자열을 화면에 보이지 않습니다.
- 로케일 순서: 사용자의 명시 설정 → 계정 설정 → 브라우저 또는 OS 설정. IP 기반 추측은 쓰지 않습니다.

### 5-7. 검증

i18n 은 숫자로 검증합니다. 읽어서 훑으면 새어 나갑니다.

- **하드코딩 문자열**: 번들을 건너뛴 사용자 노출 문자열에 대한 lint 규칙. 0 이 GREEN 입니다.
- **키 동등성**: 기본 로케일의 키 집합을 각 로케일과 비교합니다. 빠진 수와 남은 수를 보고합니다.
- **placeholder 동등성**: 키 하나의 placeholder 집합이 로케일 전체에서 같아야 합니다. 불일치는 실행
  오류입니다.
- **레이아웃 회귀**: 유사 로케일(원문을 1.4배 길이로)로 렌더링해 넘침을 좌표로 측정합니다.

이것들은 CI 에서 실행하는 노출된 명령이지 일회용 스크립트가 아닙니다.

## 6. 언어와 문체

### 6-1. 코드는 영어

**코드 안의 자연어는 예외 없이 영어입니다**: 주석, commit 메시지, 식별자, 로그, 오류 코드, 테스트
이름, API 필드 이름.

코드 안에서 한국어는 `ko` 리소스 값에만 존재합니다. 독자용 문서는 영어 정본 하나를 쓰고, 같은 위치의
`.ko.md` 경로에 한국어 번역을 둘 수 있습니다. 번역은 독립된 규칙을 정의하지 않으며 front matter 에
자기 영어 정본을 식별합니다.

검사 둘이 이것을 지킵니다. `korean_gate_test.go` 는 파일을 읽고,
`record_language_gate_test.go` 는 `git log` 를 읽습니다. 메시지는 아무도 확인하지 않던 절반이었고, 깨진
쪽이 바로 그 절반입니다 — 2026-08-16 실측으로 commit 본문이 상태 표시줄 라벨을 한국어 낱말로 담고
있었습니다. 기록 중 굳어지는 부분은 히스토리입니다. 아무도 받아 가지 않은 branch 는 아직 다시 쓸 수
있고, 그 뒤로는 쓸 수 없습니다.

### 6-2. 건조하게, 짧게, 정확하게

주석, commit 로그, 리소스 번들 값, 문서 전부에, 모든 로케일에 적용합니다.

무엇이 무엇인지, 무엇이 없는지, 무엇을 하면 되는지를 씁니다. 사실과 다음 행동, 그 밖에는 없습니다. 길수록
그중 틀린 부분이 많아집니다. 이유는 한 문장이며, 측정한 날짜를 함께 적습니다.

- 주석은 한 줄이 기본입니다. 두 줄을 넘으면 코드가 잘못된 것은 아닌지 먼저 봅니다.
- commit 제목은 한 줄입니다. 본문은 필요할 때만, 사실만.
- commit 메시지에 서사도, 경과도, 소감도 쓰지 않습니다.

### 6-3. 금지 어휘

코드를 의인화·비유·수사로 설명하지 않습니다.

목록은 `internal/repositorygate/prose_gate_test.go` 에 있습니다. 그 파일이 규칙이고 이 문단은 그것을
지목합니다. 2026-08-26 기준 한국어 48개, 영어 13개입니다. 목록은 예시입니다. 목록에 있든 없든 의인화와
시적 표현은 금지합니다. 판정: 이 문장은 사실을 진술하는가, 분위기를 만드는가?

금지된 낱말 대신 기제를 이름 짓는 낱말을 씁니다. 음차한 gate 가 아니라 검사, 스스로 행동하는 boundary 가
아니라 구분선 또는 구분, incident 가 아니라 이벤트, 감각 동사가 아니라 측정입니다.

### 6-4. 고친 예시

```
// BAD: The registry knows about every plugin that lives in the core.
// GOOD: Registry maps plugin id to instance. Core has no plugin imports.

// BAD: This value flows down to the renderer and finally reaches the view.
// GOOD: Renderer reads this value. No transform between store and view.
```

```
BAD:  feat: let the scheduler decide when workers should wake
GOOD: feat(scheduler): trigger worker on queue push

BAD:  fix: the orphan node no longer asks the parent for its position
GOOD: fix(layout): compute detached node position from viewport origin
```

```
BAD:   ko: "설정을 찾지 못했습니다. 어딘가에 있을 수 있으니 확인이 필요합니다."
GOOD:  key: config.missing
       ko: "설정 파일이 없습니다. {path}를 만들어 주세요."
       en: "Config file not found. Create {path}."
```

## 7. Git

### 7-1. Branch

- 작업은 branch 에서 합니다. main 에서 직접 하지 않습니다.
- 기능 단위로 commit 하며 영어로 씁니다.
- 작은 단위로 commit 합니다. commit 이 없으면 이 지점에 어떻게 도달했는지에 대한 기록이 없습니다.

| 상태 | 처리 |
|---|---|
| main 에 merge 됨 | `merged/` 접두사로 이름 변경 |
| 개발했지만 명시적으로 merge 하지 않음 | `backup/` 접두사로 이름 변경 |

`merged/` 와 `backup/` 은 로컬에만 둡니다.

**원격 위생**: 원격에 merge 된 branch 없음, 원격에 backup 없음. 명시적으로 요청된 branch 외에는 아무것도
없습니다.

### 7-2. Worktree

정본 체크아웃은 이 저장소 root 입니다. 작업이 정말로 필요로 할 때의 임시 worktree 는 고정된 형제 경로가
아니라 Git 으로 발견해야 하며, 그 branch 가 통합된 뒤 제거합니다. 체크아웃을 심볼릭 링크로 잇거나
자동화가 workspace 배치에 의존하게 만들지 않습니다.

### 7-4. 손실 없음

소스를 잃을 수 있는 명령은 허용하지 않으며 `git reset --hard` 도 포함합니다. 코드도 commit 도 잃지
않습니다. `git checkout -- <path>`, `git clean -fd`, `git restore` 는 commit 하지 않은 작업을 지우므로
같게 셉니다.

### 7-5. 정직한 commit 과 버전

- 무관한 것을 관련된 것으로 기록하면 기록이 거짓이 됩니다.
- 현재 작업과 무관한 옳은 수정은 진짜 이유를 적은 별도 commit 입니다.
- 이미 commit 된 실패한 가설은 그 위에 쌓는 commit 으로 제거합니다.

semver:

| 단계 | 근거 |
|---|---|
| PATCH | 하위 호환 버그 수정 |
| MINOR | 하위 호환 기능 |
| MAJOR | 호환성을 깨는 변경 |

**릴리즈 전**: 목표는 릴리즈되지 않은 상태에서 성숙한 버전 번호가 commit 된 적이 없게 하는 것입니다.
버전은 그 순간의 사실이며 주입하지 않습니다. 버전을 0.0.1 로 되돌린 것 자체는 기록할 일이 아닙니다.
처음부터 0.0.1 이었어야 합니다. 피할 수 없는 patch 증가는 허용합니다.

### 7-6. 동기화

소스, 주석, 문서, README 는 같은 상태를 유지합니다. 문서와 메모리를 갱신하는 것이 작업의 일부입니다.
코드만으로는 끝난 것이 아닙니다.

## 8. 종료 검사

여기 있는 전부를 통과해야 완료를 보고합니다. 하나라도 빠지면 완료가 아닙니다.

```sh
go tool wails3 task verify
```

검사는 단조 증가합니다. 검사가 한 번 성립하면 이후의 모든 commit 이 그때까지의 모든 검사를 통과합니다.
막을 수 없는 검사는 검사가 아닙니다. 시각 증거는 이 저장소 밖 `../evidence/<gate>/` 로 보내며, 생성된
스크린샷이 소스 파일이 되지 않게 합니다.

- [ ] RED 가 있었고 지금 GREEN 인가? 수치 근거가 있는가?
- [ ] 임시 대체 경로, 호환 경로, 우회가 남아 있지 않은가?
- [ ] 실패한 가설이 commit 에 누적되지 않았는가? 있다면 제거 commit 을 쌓았는가?
- [ ] commit 이 기능 단위로 나뉘고 메시지가 진짜 이유와 맞는가?
- [ ] 코드 안의 모든 자연어가 영어인가? 한국어가 리소스 값과 짝이 있는 `.ko.md` 번역에만 있는가?
- [ ] 사용자에게 보이는 모든 문자열이 키로 참조되는가? 날짜·숫자·통화·정렬이 로케일 API 를 지나는가?
- [ ] 키와 placeholder 동등성 확인이 모든 로케일에서 통과하는가?
- [ ] 소스, 주석, 문서, README 가 같은 상태인가?
- [ ] 버전 변경이 semver 와 맞는가?
- [ ] branch 를 그 수명주기 규칙대로 처리했는가?
- [ ] 우회가 있다면 적었는가?
- [ ] 어긴 규칙이 있다면 보고했는가?

## 9. 프로젝트 고유 규칙

충돌하는 곳에서는 위의 규칙보다 우선합니다. 충돌은 보고합니다.

- **commit 순서**: `test:` → `fix:`/`feat:` → `docs:`.
- **투명성의 세 표면**: 모든 기능이 명령·status·DOM 을 노출합니다. 셋 중 하나라도 빠지면 미완입니다.
- **캡처와 Wails MCP 서버는 관측 도구이지 판정이 아닙니다.** 통과·실패는 수치 명령이나 테스트에서
  나옵니다.
- **Windows 는 cgo 없이 유지합니다.** [`docs/tech/NATIVE-LAYER.md`](docs/tech/NATIVE-LAYER.md) 의 N3
  를 보십시오.
- **home 하나에 백엔드 하나.** 무엇을 열거나 그리기 전에 socket 을 확보합니다.
